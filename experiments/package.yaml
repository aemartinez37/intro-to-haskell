name: experiments

dependencies:
  - base

ghc-options:
  - -Wall

library:
  source-dirs: src

tests:
  spec:
    source-dirs: test
    main: Main.hs
    dependencies:
      - experiments
      - hspec
